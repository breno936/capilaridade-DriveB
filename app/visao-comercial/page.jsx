import CommercialViewClient from '../../components/commercial-view-client';

export const metadata = {
  title: 'Visao Comercial | Capilaridade das Oficinas',
  description: 'Apresente a rede credenciada DriveB para clientes, sem dados internos de gap ou operacao.',
};

export default function VisaoComercialPage() {
  return <CommercialViewClient />;
}
